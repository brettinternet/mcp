---
# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:
  format:
    desc: Format project
    cmds:
      - ruff format

  check:
    desc: Check project
    cmds:
      - ruff check
